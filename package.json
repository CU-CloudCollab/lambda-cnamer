{
  "name": "lambda-cnamer",
  "version": "1.0.0",
  "description": "An AWS Lambda function to update a Route53 CNAME record with the public DNS of an EC2 instance when it enters a RUNNING state.",
  "main": "lambda.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
      "type" : "git",
      "url" : "https://github.com/CU-CloudCollab/lambda-cnamer"
  },
  "private": true,
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.3.2"
  }
}
